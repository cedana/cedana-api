syntax = "proto3";

package comm;

service Comm {
  rpc Capture (CaptureRequest) returns (CaptureReply);
}

message CaptureRequest {
  string images_dir = 1;
}

message CaptureReply {
  string message = 1;
}

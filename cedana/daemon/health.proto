syntax = "proto3";

package daemon;

message HealthCheckReq {
  bool Full = 1; // Health check installed plugins too.
}

message HealthCheckResp {
  string Title = 1;
  string Version = 2;

  repeated string Errors = 3;
  repeated string Warnings = 4;

  repeated HealthCheckResp Plugins = 5;
}
